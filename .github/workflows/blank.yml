name: Automated API tests using Postman CLI

on: push

jobs:
  automated-api-tests:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Postman CLI
        run: |
          powershell.exe -NoProfile -InputFormat None -ExecutionPolicy AllSigned -Command "[System.Net.ServicePointManager]::SecurityProtocol = 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://dl-cli.pstmn.io/install/win64.ps1'))"
      - name: Login to Postman CLI
        run: postman login --with-api-key ${{ secrets.PMAK-650f1db6445d8900311459d1-d4e95af6629fa7ee4c4b4f0f18eeff6917 }}
      - name: Run API tests
        run: |
          postman collection run "19522231-7afa6da5-8f6c-4901-b3a7-a6660ab5b4c3" -e "19522231-9ed00ecf-a298-4e15-81c9-b9636a250aa3"
